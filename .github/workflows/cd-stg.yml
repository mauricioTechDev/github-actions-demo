name: stg deploy
on:
  release:
    types: [prereleased]
    
env:
  ENVIRONMENT: stg
    
jobs:
  deploy-app:
    runs-on: ubuntu-latest
    environment: "${{ env.ENVIRONMENT }}"
    steps:
      - run: echo "This is deploying to the ${{ env.ENVIRONMENT }} environment with the github tag ${{ github.ref }}"
